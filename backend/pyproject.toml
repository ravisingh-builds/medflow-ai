[project]
name = "medflow-ai-backend"
version = "0.1.0"
description = "AI-powered healthcare lead recovery platform"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.116.0",
    "uvicorn[standard]>=0.35.0",
    "sqlalchemy>=2.0.40",
    "alembic>=1.16.0",
    "psycopg[binary]>=3.2.0",
    "pydantic-settings>=2.10.0",
]
